#
# Comando WHILE
#

# Entrada dos números

m = float(input('Digite o primeiro número: '))
n = float(input('Digite o segundo número: '))

# Teste de ordem de digitação.

if ( m > n ):
    troca = m
    m = n
    n = troca

# Laço de repetição.

soma = 0

while ( m <= n ):
    soma = soma + m
    m = m + 1

print("O resultado é: " + str( soma ))
