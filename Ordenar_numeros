# Rotina para ordenar 4 números de forma crescente.

# Entrada dos números

a = float(input('Digite o primeiro número: '))
b = float(input('Digite o segundo número: '))
c = float(input('Digite o terceirod número: '))
d = float(input('Digite o quarto número: '))

# Comparação dos números.

if ( a <= b ):
    t = a
    a = b
    b = t

if ( a <= c ):
    t = a
    a = c
    c = t

if ( a <= d ):
    t = a
    a = d
    d = t

if ( b <= c ):
    t = b
    b = c
    c = t

if ( b <= d ):
    t = b
    b = d
    d = t

if ( c <= d ):
    t = c
    c = d
    d = t

# Impressão dos números.

print("O primeiro número é " + str(a))
print("O segundo número é " + str(b))
print("O terceiro número é " + str(c))
print("O quarto número é " + str(d))
